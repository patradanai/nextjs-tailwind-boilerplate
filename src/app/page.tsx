import Button from '@/components/ui/Button/Button'

export default function Home() {
    return (
        <main className="font-kanit">
            <Button size="md" variant="contained" color={'success'} disabled>
                ERROR
            </Button>
        </main>
    )
}
